<div class="container m-4 w-800">
<h3 class="m-4">Choisissez votre restaurant</h3>

<ul class="m-5   p-1">
  <% @restaurants.each do |restaurant| %>
    <li class="m-2"><%= link_to restaurant.name, restaurant_path(restaurant) %></li>
  <% end %>
</ul>
</div>

<%= link_to 'Add a restaurant', new_restaurant_path %>
